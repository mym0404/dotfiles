# zmodload zsh/zprof
export ZSH="$HOME/.oh-my-zsh"
 
ZSH_THEME="lambda-gitster"
 
MJ_MACHINE="1"
alias cat='bat'
export PAGER='bat --color=always'
export MANPAGER='bat --color=always'
 
# key speed
defaults write -g InitialKeyRepeat -float 11.0
defaults write -g KeyRepeat -float 1.1
# animation disable
defaults write com.apple.dock launchanim -bool false
defaults write com.apple.dock expose-animation-duration -float 0.1
defaults write com.apple.dock mineffect -string "scale"
defaults write com.apple.dock autohide-delay -float 0
defaults write com.apple.dock autohide-time-modifier -float 0
defaults write NSGlobalDomain NSWindowResizeTime -float 0.001
# show hidden file in finder selector
defaults write com.apple.finder AppleShowAllFiles -bool true
 
# zstyle ':omz:update' mode auto
plugins=(
  git
  zsh-autosuggestions
  direnv
  fzf
)
 
export XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
alias g=git
 
source $ZSH/oh-my-zsh.sh
 
eval "$(fzf --zsh)"
 
alias vim='nvim'
alias v='nvim'
alias python='python3'
alias pip='pip3'
export EDITOR=nvim
alias VIM="cd ~/.config/nvim && vim ."
alias ws=webstorm
 
#AI
export CODEX_HOME=~/.codex
alias MISE="cd ~/.config/mise"
alias x='codex'
 
export PATH="$HOME/.fastlane/bin:$PATH"
export PATH="$HOME/.local/bin:$PATH"

export ANDROID_HOME="$HOME/Library/Android/sdk"
export PATH="$PATH:$ANDROID_HOME/emulator"
export PATH="$PATH:$ANDROID_HOME/platform-tools"
 
export PATH="$PATH:/usr/local/bin"
export PATH="/opt/homebrew/bin:$PATH"
 
export CC=/usr/bin/gcc
export CXX=/usr/bin/g++
 
#SHORT CUT
alias D='cd ~/Desktop'
alias PRO='cd ~/projects'
alias DOWN='cd ~/Downloads'
alias APP='cd /Applications'
alias WEZ='vim ~/.wezterm.lua'
alias CODEX='cd ~/.codex'
alias ZED='cd ~/.config/zed'
alias P='vim ~/.zshrc'
alias S='exec zsh'
alias IP='ifconfig | grep "inet "'
alias re='ssh -i "/Users/mj/Library/Mobile Documents/com~apple~CloudDocs/OracleSSH/oracle.key" ubuntu@server.mjstudio.net'
 
#FAVORITE
alias MJ='cd ~/projects/MJStudio-Web/MJStudio-React'
 
alias ALGO='cd /Users/mj/projects/ps-blog'
alias EN='cd /Users/mj/projects/english'
alias ES='EN && git pull && pnpm sync && PUSH'
alias ARMY='PRO && cd army-apply'
alias SUITED='PRO && cd suited'
alias GR='PRO && cd granite'
#GITHUB
alias GIT='vim ~/.gitconfig'
DEFAULT_CM_MSG='chore: unconscious commit'
alias CC='git add -A && git status && git commit --allow-empty -m "chore: unconscious commit 🎲"'
alias PUSH='CC && g ps'
alias GH="git config --get remote.origin.url | sed 's/git@github.com:/https:\/\/github.com\//' | sed 's/.git$//' | xargs open"
alias PR="gh pr view -w"
alias f=lazygit
 
# export PYTORCH_ENABLE_MPS_FALLBACK=1
 
# kill port util
kp() {
  if [ -z "$1" ]; then
    echo "usage: kp <port>"
    return 1
  fi
 
  local pids
  pids=$(lsof -ti tcp:"$1")
 
  if [ -z "$pids" ]; then
    echo "no process on port $1"
    return 0
  fi
 
  echo "$pids" | xargs kill
  echo "killed port $1: $pids"
}
#XCODE
alias XCODE='cd ~/Library/Developer/Xcode'
 
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
 
# Generated for envman. Do not edit.
[ -s "$HOME/.config/envman/load.sh" ] && source "$HOME/.config/envman/load.sh"
 
# figlet -f "Sub-Zero" "MJ" | lolcat
 
ZSH_AUTOSUGGEST_STRATEGY=(history completion)
 
# zprof
 
# mise
eval "$(mise activate zsh)"

alias -g -- -h='-h 2>&1 | bat --language=help --style=plain'
alias -g -- --help='--help 2>&1 | bat --language=help --style=plain'
