interface:
  display_name: "Grill Me"
  short_description: "Stress-test every ready decision in focused rounds."
  default_prompt: "Use $grill-me to relentlessly stress-test this plan in dependency-aware rounds before implementation."

policy:
  allow_implicit_invocation: false
