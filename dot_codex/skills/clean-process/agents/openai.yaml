interface:
  display_name: "Clean Process"
  short_description: "Clean stale macOS automation, development, and build processes."
  default_prompt: "Use $clean-process to inspect and clean stale macOS automation, development, and resource-heavy build processes."

policy:
  allow_implicit_invocation: false
