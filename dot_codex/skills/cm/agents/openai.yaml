interface:
  display_name: "Commit Recent Work"
  short_description: "Commit the latest task from this conversation."
  default_prompt: "Use $cm to commit only the latest completed task from this conversation."

policy:
  allow_implicit_invocation: false
